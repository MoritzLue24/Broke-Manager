info:
  name: create-transaction
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/transactions"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "type": "income",
        "amount": 250,
        "title": "Gehalt",
        "counterParty": "",
        "description": "",
        "date": "2026-06-16"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
