info:
  name: create-transaction
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/transactions"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
