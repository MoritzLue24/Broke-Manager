info:
  name: create-category
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/categories"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Essen",
        "keywords": ["rewe", "edeka"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
