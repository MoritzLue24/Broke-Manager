info:
  name: create-category
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/categories"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Pornhub2",
        "keywords": ["sex", "porn"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
