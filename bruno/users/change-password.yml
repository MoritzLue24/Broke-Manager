info:
  name: change-password
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/users/me/change-password"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "CurrentPassword": "4asd",
        "NewPassword": "5asd",
        "ConfirmNewPassword": "5asd"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
