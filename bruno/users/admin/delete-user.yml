info:
  name: delete-user
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/users/:userId"
  params:
    - name: userId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
