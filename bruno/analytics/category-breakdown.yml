info:
  name: category-breakdown
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/analytics/category-breakdown"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
