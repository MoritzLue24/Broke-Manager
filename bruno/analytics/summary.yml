info:
  name: summary
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/analytics/summary"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
